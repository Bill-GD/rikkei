<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title; %></title>
  </head>
  <body>
    <ol>
      <% tags.forEach(e => { %>
        <li><%= e.name %></li>
      <% }) %>
    </ol>
  </body>
</html>
